This directory intentionally does not contain raw NSDUH data.

The National Survey on Drug Use and Health (NSDUH) data are subject to
use restrictions and must be obtained directly from SAMHSA.

To reproduce this analysis:
1. Obtain NSDUH 2024 data
2. Load the dataset as `ds` in R
3. Run the scripts in numerical order
